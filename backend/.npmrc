# EB sets NODE_ENV=production by default, which makes npm skip devDependencies.
# We need devDeps (typescript, @types/*) to run "npm run build" during deploy,
# so override it for the install step.
production=false
